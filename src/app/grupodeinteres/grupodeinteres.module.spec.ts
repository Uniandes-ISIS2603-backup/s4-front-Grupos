import { GrupodeinteresModule } from './grupodeinteres.module';

describe('GrupodeinteresModule', () => {
  let grupodeinteresModule: GrupodeinteresModule;

  beforeEach(() => {
    grupodeinteresModule = new GrupodeinteresModule();
  });

  it('should create an instance', () => {
    expect(grupodeinteresModule).toBeTruthy();
  });
});
